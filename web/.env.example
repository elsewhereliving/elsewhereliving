# ---------------------------------------------------------------------------
# Elsewhere Living — environment
# ---------------------------------------------------------------------------

# Optional: interactive maps. Leave blank to use the free OpenStreetMap maps.
PUBLIC_GOOGLE_MAPS_KEY=

# ---------------------------------------------------------------------------
# These are NOT needed locally — set them in the Cloudflare Pages dashboard
# (Settings → Environment variables). They power the GitHub login for /admin.
# See DEPLOY.md → "Turn on editing".
#   GITHUB_CLIENT_ID=...
#   GITHUB_CLIENT_SECRET=...
# ---------------------------------------------------------------------------
